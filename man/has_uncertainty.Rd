% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{has_uncertainty}
\alias{has_uncertainty}
\title{Check whether a \verb{<dist_spec>} is uncertain}
\usage{
has_uncertainty(x, id = NULL)
}
\arguments{
\item{x}{A \verb{<dist_spec>}.}

\item{id}{Integer; the id of the distribution to use (if x is a composite
distribution). If \code{x} is a single distribution this is ignored and can be
left at its default value of \code{NULL}.}
}
\value{
\code{TRUE} if the (component) distribution is uncertain.
}
\description{
A distribution is uncertain when it carries a prior: a parametric
distribution with a \verb{<dist_spec>} (rather than numeric) parameter, or a
nonparametric distribution whose PMF is given by a \verb{<dist_spec>} (a
\code{\link[=Dirichlet]{Dirichlet()}} prior) rather than a numeric vector.
}
\examples{
has_uncertainty(Gamma(shape = 1, rate = 1))
has_uncertainty(Gamma(shape = Normal(1, 0.5), rate = 1))
}
\keyword{internal}
