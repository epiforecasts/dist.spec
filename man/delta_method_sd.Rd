% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{delta_method_sd}
\alias{delta_method_sd}
\title{Delta-method standard deviations of the natural parameters}
\usage{
delta_method_sd(x, sds, natural, h_rel = 1e-04)
}
\arguments{
\item{x}{A single \verb{<dist_spec>} whose parameters are the unnatural parameters
evaluated at their means.}

\item{sds}{Numeric; the standard deviation of each unnatural parameter, in
\code{names(x$parameters)} order (\code{0} for a fixed parameter).}

\item{natural}{The natural-parameter list evaluated at the means.}

\item{h_rel}{Numeric; the relative step of the central finite difference. For
parameter \code{i} the step is \code{h_rel * max(abs(mean_i), 1)}, i.e. relative to
the parameter value with an absolute floor near zero. The default \code{1e-4}
keeps both the truncation error (order \code{h^2}) and the floating-point
cancellation error (order \code{eps / h}) small for the smooth \code{\link[=to_natural]{to_natural()}}
maps. It is a numerical-differentiation constant exposed here for testing,
deliberately kept off the user-facing constructors.}
}
\value{
A named numeric vector of standard deviations, one per natural
parameter.
}
\description{
First-order (delta-method) standard deviations of a distribution's natural
parameters, propagated from uncertain unnatural parameters. The uncertain
unnatural parameters are treated as independent normals with standard
deviations \code{sds}, and for each natural parameter the propagated standard
deviation is \verb{sqrt(sum_i J[j, i]^2 * sds[i]^2)}, where the Jacobian
\code{J[j, i] = d(natural_j) / d(param_i)} is estimated by central finite
differences. Estimating the Jacobian numerically lets this work uniformly for
every distribution type, including the Weibull, whose \code{\link[=to_natural]{to_natural()}} solves
for the shape numerically.
}
\keyword{internal}
