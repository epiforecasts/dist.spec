% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{Distributions}
\alias{Distributions}
\title{Probability distributions}
\description{
distspec represents probability distributions (typically epidemiological
delays, such as generation times or reporting delays) as \verb{<dist_spec>}
objects. Each supported distribution has its own constructor: \code{\link[=LogNormal]{LogNormal()}},
\code{\link[=Gamma]{Gamma()}}, \code{\link[=Normal]{Normal()}}, \code{\link[=Exp]{Exp()}}, \code{\link[=Weibull]{Weibull()}}, \code{\link[=Beta]{Beta()}}, \code{\link[=Fixed]{Fixed()}},
\code{\link[=NonParametric]{NonParametric()}} and \code{\link[=Dirichlet]{Dirichlet()}}.
}
\details{
A parameter can be given either as a fixed numeric value or as an uncertain
value (another \verb{<dist_spec>}); currently only normally distributed uncertain
parameters (from \code{\link[=Normal]{Normal()}}) are supported.

Each distribution has a "natural" (canonical) parameterisation, such as
\code{shape} and \code{rate} for \code{\link[=Gamma]{Gamma()}} or \code{meanlog} and \code{sdlog} for \code{\link[=LogNormal]{LogNormal()}}.
It can sometimes also be specified using other parameters, such as its mean
and standard deviation, which are then converted to the natural parameters
(propagating any uncertainty with a first-order delta-method approximation).
}
\seealso{
\code{\link[=discretise]{discretise()}} and \code{\link[=collapse]{collapse()}} to discretise and convolve
distributions, \code{\link[=sample_dist]{sample_dist()}} to draw samples, and \code{\link[=get_parameters]{get_parameters()}} /
\code{\link[=get_pmf]{get_pmf()}} to inspect them.
}
