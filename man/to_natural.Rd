% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{to_natural}
\alias{to_natural}
\title{Convert a distribution's parameters to its natural parameters (per-type)}
\usage{
to_natural(x, ux)
}
\arguments{
\item{x}{A single \verb{<dist_spec>}.}

\item{ux}{The parameter means, as returned by \code{lapply(x$parameters, mean)}.}
}
\value{
A named list of natural parameters.
}
\description{
Per-type conversion of a distribution's parameters to its natural parameters.
Dispatched on the distribution type; each method reads the parameter means
from \code{ux} and returns the natural parameters as a named list (see e.g.
\code{to_natural.gamma}). The shared pre- and post-processing lives in
\code{\link[=convert_to_natural]{convert_to_natural()}}, which computes \code{ux} once and passes it in.
}
\keyword{internal}
