% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_spec.R
\name{sample_dist}
\alias{sample_dist}
\alias{sample_dist.dist_spec}
\alias{sample_dist.multi_dist_spec}
\title{Sample from a distribution}
\usage{
sample_dist(x, n, ...)

\method{sample_dist}{dist_spec}(x, n, ...)

\method{sample_dist}{multi_dist_spec}(x, n, ...)
}
\arguments{
\item{x}{A \verb{<dist_spec>}.}

\item{n}{The number of samples to draw.}

\item{...}{Not used.}
}
\value{
A numeric vector of \code{n} samples.
}
\description{
Draws random samples from a \verb{<dist_spec>} whose parameters are fixed numbers,
using the base-R random-generation function for its family (e.g. \code{\link[=rgamma]{rgamma()}}
for a gamma distribution). A discretised distribution is sampled on its
integer support.

Only distributions with fixed parameters can be sampled. If any parameter is
itself a distribution (a prior), there is no single distribution to sample
from and an error is raised.
}
\examples{
# Samples from a fixed gamma distribution
sample_dist(Gamma(shape = 2, rate = 1), 10)

# Samples from a discretised distribution, drawn on its integer support
sample_dist(discretise(Gamma(shape = 2, rate = 1, max = 20)), 10)

# A fixed distribution always returns the same value
sample_dist(Fixed(3), 5)
}
