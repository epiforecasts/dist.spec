% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{Dirichlet}
\alias{Dirichlet}
\title{Dirichlet prior for a nonparametric distribution}
\usage{
Dirichlet(alpha, prior, concentration, ...)
}
\arguments{
\item{alpha}{A positive numeric vector of concentration parameters.}

\item{prior}{Either a numeric PMF vector (zero-indexed, i.e. the
first entry represents probability mass at zero) or a
\code{dist_spec} object. If a \code{dist_spec} object is provided it will
be discretised and the PMF extracted. If numeric, it will be
normalised to sum to one internally.}

\item{concentration}{A positive scalar controlling how tightly
the Dirichlet prior concentrates around the supplied PMF.
The Dirichlet alpha vector is computed as
\code{alpha_i = concentration * p_i} where \code{p_i} is the prior PMF.
Guidance on values:
\itemize{
\item \code{concentration = 1}: weak prior, each alpha equals the PMF
value (near-uniform for roughly equal PMF entries)
\item \code{concentration = 5-20}: moderate flexibility around the
reference shape
\item \verb{concentration = 50+}: strong anchoring to the reference PMF
}}

\item{...}{Not used.}
}
\value{
A \verb{<dist_spec>}.
}
\description{
A Dirichlet prior over the weights of a nonparametric probability mass
function, used to specify an estimated \code{\link[=NonParametric]{NonParametric()}} distribution whose
PMF is estimated during model fitting. Give either \code{alpha} directly, or a
reference \code{prior} PMF together with a \code{concentration}.
}
\examples{
Dirichlet(c(1, 1, 1, 1))
Dirichlet(prior = c(0.1, 0.3, 0.4, 0.2), concentration = 10)
}
\seealso{
\code{\link[=NonParametric]{NonParametric()}} to use the prior, and \link{Distributions} for an
overview.
}
